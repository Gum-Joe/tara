/**
 * @overview Tara's explorer entry point
 * @module tara/explorer
 */

const main = (tara) => {
  tara.logger.info("Explorer started.");
};

const client = "<JSX />";

module.exports = {
  client,
  main
};
